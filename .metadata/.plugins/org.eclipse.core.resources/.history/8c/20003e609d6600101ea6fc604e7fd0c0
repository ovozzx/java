package oop;

public class Character {
	String nickname;
	int health;
	int damage;
	boolean isDead;
	
	void attack() {
		int lucky = (int) Math.random() * 100; // 0~99 
		int attackDamage = damage;
		if (lucky > 70) {
			
		}
	}
	
}
