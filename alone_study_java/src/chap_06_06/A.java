package chap_06_06; // 패키지 선언 : 해당 클래스가 어떤 패키지에 속할 것인지 선언

//import문 작성 위치 

public class A {
	
	/*
	 * - 패키지 : 파일 시스템의 폴더 형태, 클래스 이름이 같아도 패키지가 다르면 다른 클래스로 인식 
	 * - 클래스 전체 이름 -> 상위패키지.하위패키지.클래스 
	 * - 패키지는 클래스의 일부, 클래스만 복사해서 다른 곳으로 이동하면 사용할 수 없음 ! 패키지 전체를 이동시켜야 함 
	 * - new package -> name : sec06.exam01 
	 * */

}
